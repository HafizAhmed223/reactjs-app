First run the ec2 instance 
then copy the Public IP address of the Instance
Update the REACT_APP_BASE_URL in the secrets and add :4000 after the ppublic ip address
Update the REACT_APP_BASE_URL in the env of the code base
then run the workflow
